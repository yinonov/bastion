{
  "name": "bastion-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@8.5.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --parallel --filter @bastion/edge --filter @bastion/dashboard dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  }
}
