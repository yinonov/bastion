---
phase: 05-dogfood-evidence-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/edge/src/store.ts
  - packages/cli/src/index.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/05-dogfood-evidence-closure/EVIDENCE.md
  - .planning/phases/05-dogfood-evidence-closure/05-01-SUMMARY.md
  - .planning/phases/05-dogfood-evidence-closure/05-VERIFICATION.md
autonomous: true
requirements:
  - DOG-01
must_haves:
  truths:
    - "A persisted security finding can be exported together with its linked event metadata from the local SQLite store"
    - "Phase 5 records one concrete DOG-01 evidence artifact with timestamp, redacted snippet, and finding details"
    - "Traceability documents mark DOG-01 complete only after the evidence artifact exists"
  artifacts:
    - path: "packages/edge/src/store.ts"
      provides: "Finding-to-event evidence lookup for exported dogfood artifacts"
    - path: "packages/cli/src/index.ts"
      provides: "CLI command to export one persisted finding as a DOG-01 evidence payload"
    - path: ".planning/phases/05-dogfood-evidence-closure/EVIDENCE.md"
      provides: "Human-readable exported DOG-01 evidence"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/edge/src/store.ts"
      via: "export-finding command calling store evidence lookup"
      pattern: "local CLI -> SQLite finding/event pair -> deterministic artifact"
---

<objective>
Close DOG-01 by exporting a real persisted security finding from Bastion dogfood usage and recording the traceable evidence needed for verification.

Purpose: The codebase already captures findings, but Phase 5 needs a repeatable way to surface one organic catch as an explicit artifact and then mark the requirement complete.
Output: CLI evidence export, captured EVIDENCE.md, verification record, and requirements/roadmap/state updates.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-dogfood-evidence-closure/05-CONTEXT.md
@packages/edge/src/store.ts
@packages/cli/src/index.ts

Existing local evidence path:
- `LocalSqliteStore.recentFindings()` reads persisted findings
- `LocalSqliteStore.recentEvents()` reads persisted agent events
- `bastion export-report` already proves CLI-driven local export is an established pattern
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a focused SQLite evidence lookup for persisted findings</name>
  <files>packages/edge/src/store.ts</files>
  <action>Add a small store method that returns one persisted finding and its linked event by finding id. Keep it local-first and reuse the existing row parsers instead of adding a new persistence path.</action>
  <verify>
    <automated>pnpm --filter @bastion/cli typecheck</automated>
  </verify>
  <done>The store can return a single dogfood finding with traceable event metadata for export.</done>
</task>

<task type="auto">
  <name>Task 2: Expose DOG-01 evidence export as a CLI command</name>
  <files>packages/cli/src/index.ts</files>
  <action>Add `bastion export-finding <id>` with optional `--output <path>` and `--format <markdown|json>` so one persisted finding can be exported reproducibly for phase evidence capture.</action>
  <verify>
    <automated>pnpm --filter @bastion/cli typecheck</automated>
  </verify>
  <done>The CLI can export a redacted finding artifact directly from the local store.</done>
</task>

<task type="auto">
  <name>Task 3: Capture DOG-01 evidence and update traceability</name>
  <files>.planning/phases/05-dogfood-evidence-closure/EVIDENCE.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/05-dogfood-evidence-closure/05-VERIFICATION.md</files>
  <action>Use the new export path against the existing local SQLite data, record the evidence artifact, mark DOG-01 complete, and update roadmap/state to show Phase 5 complete.</action>
  <verify>
    <automated>pnpm --filter @bastion/cli bastion export-finding <captured-id> --format json</automated>
  </verify>
  <done>Phase 5 has a concrete evidence artifact and all planning docs reflect DOG-01 closure.</done>
</task>

</tasks>

<verification>
- `pnpm --filter @bastion/cli typecheck`
- `pnpm --filter @bastion/cli bastion export-finding <captured-id> --format json`
</verification>

<success_criteria>
1. One persisted finding can be exported with its linked event metadata from the local store.
2. Phase 5 records a real DOG-01 evidence artifact with timestamp, severity, redacted snippet, and event linkage.
3. Requirements, roadmap, and state all mark DOG-01 and Phase 5 complete only after the artifact exists.
</success_criteria>

<output>
After completion, create `.planning/phases/05-dogfood-evidence-closure/05-01-SUMMARY.md` and `.planning/phases/05-dogfood-evidence-closure/05-VERIFICATION.md`.
</output>