---
phase: 03-intelligence-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/core/src/hooks.ts
  - packages/insights/src/index.ts
  - packages/insights/src/insights.test.ts
  - packages/edge/src/server.ts
  - packages/edge/src/server.test.ts
autonomous: true
requirements:
  - INS-02
  - INS-04
  - DASH-02
must_haves:
  truths:
    - "Shadow spend is computed from captured UserPromptSubmit token counts and pricing constants, not redacted snippet length"
    - "Insights recomputation is scheduled asynchronously after ingestion and does not block hook response generation"
    - "Risk score, blocked count, secrets count, and estimated spend can be verified against direct SQLite-backed calculations"
  artifacts:
    - path: "packages/core/src/hooks.ts"
      provides: "Prompt token-count extraction into normalized event metadata"
    - path: "packages/insights/src/index.ts"
      provides: "Token-count based spend estimator and risk/spend helpers"
    - path: "packages/edge/src/server.ts"
      provides: "Asynchronous insights refresh trigger on ingest routes"
    - path: "packages/edge/src/server.test.ts"
      provides: "Integration checks for summary metric accuracy and non-blocking ingest behavior"
  key_links:
    - from: "packages/core/src/hooks.ts"
      to: "packages/insights/src/index.ts"
      via: "metadata prompt token fields consumed by estimateShadowSpend"
      pattern: "UserPromptSubmit token metadata -> USD estimator"
    - from: "packages/edge/src/server.ts"
      to: "packages/edge/src/store.ts"
      via: "async refresh trigger after saveEvent/saveFindings"
      pattern: "ingest response path returns before heavy insights recompute completes"
---

<objective>
Make spend and summary metrics mathematically trustworthy by using captured token counts and asynchronous insights recomputation that preserves hook-path performance.

Purpose: Satisfy INS-02, INS-04, and DASH-02 with verifiable backend behavior tied directly to live SQLite-backed data.
Output: Token-aware normalization and spend estimation, async insights scheduling in ingest routes, and end-to-end metric tests.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-intelligence-dashboard/03-CONTEXT.md
@.planning/phases/03-intelligence-dashboard/03-01-PLAN.md
@packages/core/src/hooks.ts
@packages/insights/src/index.ts
@packages/edge/src/server.ts

<interfaces>
From packages/core/src/hooks.ts:
```typescript
metadata: {
  permissionMode: payload.permission_mode,
  transcriptPath: payload.transcript_path,
  toolUseId: payload.tool_use_id,
  command,
  file_path: filePath,
  path: toolInput.path,
  promptLength: prompt?.length ?? 0
}
```

From packages/insights/src/index.ts:
```typescript
export function estimateShadowSpend(events: AgentEvent[]): number {
  const chars = events.reduce((total, event) => total + (event.redactedSnippet?.length ?? 0), 0);
  const estimatedTokens = chars / 4;
  return Number(((estimatedTokens / 1_000_000) * 3).toFixed(4));
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize prompt token metadata and switch spend estimator to token-count inputs</name>
  <files>packages/core/src/hooks.ts, packages/insights/src/index.ts, packages/insights/src/insights.test.ts</files>
  <behavior>
    - UserPromptSubmit normalization captures prompt token counts from hook payload fields into event metadata.
    - Spend estimator sums captured token counts for relevant events and applies explicit Anthropic pricing constants.
    - Insights tests prove spend output tracks token-count changes and no longer depends on snippet-length heuristics.
  </behavior>
  <action>Implement D-05 by extracting token-count candidates from hook payload (for example top-level, nested metadata, or usage fields) into normalized event metadata and update `estimateShadowSpend` to consume those counts with explicit pricing constants. Keep backward-compatible fallback semantics explicit and tested so existing events without token counts do not crash calculations.</action>
  <verify>
    <automated>pnpm --filter @bastion/insights test</automated>
  </verify>
  <done>Shadow spend is derived from captured token counts and verified by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run insights refresh asynchronously after ingest without blocking hook replies</name>
  <files>packages/edge/src/server.ts, packages/edge/src/server.test.ts</files>
  <behavior>
    - Hook and MCP ingest routes schedule insights refresh off the synchronous response path.
    - Hook decision/JSON-RPC responses remain structurally identical while intelligence data updates shortly after persistence.
    - Tests prove summary totals (risk/blocked/secrets/spend) match store-backed expectations after ingest.
  </behavior>
  <action>Implement D-06 by scheduling the new store insights refresh path asynchronously after `saveEvent`/`saveFindings` in edge ingest handlers, with guarded error handling that does not fail incoming requests. Extend integration tests to assert both response correctness and metric parity against direct store data once the async refresh settles.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Ingest routes remain fast and response-correct while insights and summary metrics update from asynchronous processing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude hook payload -> normalization metadata | Untrusted external payload fields influence token-based spend calculations |
| ingest route hot path -> async insights worker | Async processing boundary can fail independently of request-response behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | `packages/core/src/hooks.ts` metadata extraction | mitigate | Strictly coerce numeric token fields and ignore invalid/non-finite values to prevent metric poisoning |
| T-03-05 | D | `packages/edge/src/server.ts` async refresh path | mitigate | Run refresh in guarded async scheduler with bounded batch size so hook responses cannot be blocked by analytics work |
| T-03-06 | R | `packages/edge/src/server.test.ts` summary metrics | mitigate | Add deterministic parity assertions between API summary totals and direct store-derived expected values |
</threat_model>

<verification>
- `pnpm --filter @bastion/insights test`
- `pnpm --filter @bastion/edge test`
</verification>

<success_criteria>
1. Estimated spend is token-count based and validated by tests.
2. Insights recomputation is asynchronous and does not alter hook/MCP response contracts.
3. Summary metrics can be cross-checked against direct store-backed expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-dashboard/03-02-SUMMARY.md`.
</output>
