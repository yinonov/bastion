---
phase: 07-integration-hardening-follow-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/lib/api.ts
  - apps/dashboard/lib/types.ts
  - apps/dashboard/components/live-dashboard.tsx
  - packages/core/src/schemas.ts
  - packages/edge/src/store.ts
  - packages/edge/src/server.ts
  - packages/edge/src/server.test.ts
  - bastion.config.example.json
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/07-integration-hardening-follow-ups/07-01-SUMMARY.md
  - .planning/phases/07-integration-hardening-follow-ups/07-VERIFICATION.md
autonomous: true
requirements:
  - DASH-01
must_haves:
  truths:
    - "Dashboard report download uses the configured edge URL, not a hardcoded localhost URL"
    - "Latency metrics are included in summary payload and rendered deterministically from SSR data"
    - "MCP transport support is explicit: http proxy allowed, stdio registered-but-not-proxied, unsupported transports rejected by config validation"
  artifacts:
    - path: "apps/dashboard/lib/api.ts"
      provides: "Shared configured edge/report URL helpers"
    - path: "apps/dashboard/components/live-dashboard.tsx"
      provides: "SSR-backed latency rendering and report link parity"
    - path: "packages/edge/src/store.ts"
      provides: "Summary payload includes latency metrics"
    - path: "packages/edge/src/server.ts"
      provides: "Explicit MCP transport handling behavior"
    - path: "packages/edge/src/server.test.ts"
      provides: "Transport behavior and summary latency coverage"
---

<objective>
Close phase 7 integration hardening gaps by removing dashboard URL drift, stabilizing latency initial render state, and making MCP transport behavior explicit and test-covered.

Purpose: Align runtime behavior with configuration and documentation while keeping the edge hot-path stable.
Output: Code + tests + phase artifacts that satisfy all phase 7 success criteria.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Unify dashboard edge/report URL resolution</name>
  <files>apps/dashboard/lib/api.ts, apps/dashboard/components/live-dashboard.tsx</files>
  <action>Export URL helpers from the dashboard API layer and consume them in the live dashboard report download link so all edge calls use the same configured base URL.</action>
  <done>Report download points to configured edge base URL instead of hardcoded localhost.</done>
</task>

<task type="auto">
  <name>Task 2: Make latency panel deterministic on initial render</name>
  <files>apps/dashboard/lib/types.ts, apps/dashboard/lib/api.ts, packages/core/src/schemas.ts, packages/edge/src/store.ts, apps/dashboard/components/live-dashboard.tsx, packages/edge/src/server.test.ts</files>
  <action>Add latency metrics to DashboardSummary and render latency cards from SSR summary data, then keep values updated through the existing polling refresh.</action>
  <done>No SSR loading ambiguity for latency panel and summary payload includes latency metrics.</done>
</task>

<task type="auto">
  <name>Task 3: Make MCP transport scope explicit and verified</name>
  <files>packages/edge/src/server.ts, packages/edge/src/server.test.ts, bastion.config.example.json</files>
  <action>Keep only HTTP proxying active, return explicit JSON-RPC failure for stdio registration, and add tests/docs that clarify supported/unsupported transports in v1.</action>
  <done>MCP transport behavior is explicit, documented, and covered by automated tests.</done>
</task>

</tasks>

<verification>
- pnpm --filter @bastion/core test
- pnpm --filter @bastion/edge test
- pnpm --filter @bastion/dashboard typecheck
</verification>

<success_criteria>
1. Dashboard report download uses configured edge base URL instead of hardcoded localhost.
2. MCP transport behavior is explicit and test-covered for supported/unsupported transports.
3. Latency panel has deterministic initial render state without SSR blank/loading ambiguity.
</success_criteria>

<output>
After execution, create:
- .planning/phases/07-integration-hardening-follow-ups/07-01-SUMMARY.md
- .planning/phases/07-integration-hardening-follow-ups/07-VERIFICATION.md
</output>
