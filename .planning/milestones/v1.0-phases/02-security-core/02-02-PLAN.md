---
phase: 02-security-core
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/core/src/hooks.ts
  - packages/edge/src/store.ts
  - packages/edge/src/server.ts
  - packages/edge/src/server.test.ts
autonomous: true
requirements:
  - SEC-05
  - SEC-06
  - SEC-07
must_haves:
  truths:
    - "Claude hook events persist findings to SQLite with UUID, timestamp, eventId, type, severity, title, description, evidence snippet, and recommendation"
    - "Claude Code receives valid response envelopes for allow, deny, ask, and redact decisions"
    - "Stored findings contain redacted evidence only unless raw payload capture is explicitly enabled"
  artifacts:
    - path: "packages/core/src/hooks.ts"
      provides: "Claude hook response adapter that matches policy decisions to Claude-compatible envelopes"
    - path: "packages/edge/src/server.ts"
      provides: "Hook ingress that normalizes payloads, evaluates policy, persists events/findings, and returns the correct response"
    - path: "packages/edge/src/store.ts"
      provides: "SQLite persistence contract for SecurityFinding records"
    - path: "packages/edge/src/server.test.ts"
      provides: "End-to-end verification of hook ingress, decision envelopes, and findings persistence"
  key_links:
    - from: "packages/core/src/hooks.ts"
      to: "packages/edge/src/server.ts"
      via: "toClaudeHookResponse(normalized event, decision)"
      pattern: "valid hook response shape per event type"
    - from: "packages/edge/src/server.ts"
      to: "packages/edge/src/store.ts"
      via: "saveEvent + saveFindings"
      pattern: "SQLite contains sanitized finding rows for hook decisions"
---

<objective>
Wire the Claude hook ingress path so Phase 2 policy decisions become valid Claude responses and durable SQLite findings.

Purpose: Complete the local enforcement path for real Claude Code events before expanding to MCP proxy behavior.
Output: Updated hook adapter, edge route, SQLite store persistence, and end-to-end tests for hook decisions and findings.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-security-core/02-CONTEXT.md
@.planning/phases/02-security-core/02-01-PLAN.md
@packages/core/src/hooks.ts
@packages/edge/src/server.ts
@packages/edge/src/store.ts

<interfaces>
From packages/core/src/hooks.ts:
```typescript
export type ClaudeHookResponse = {
  continue?: boolean;
  stopReason?: string;
  suppressOutput?: boolean;
  systemMessage?: string;
  hookSpecificOutput?: {
    hookEventName: string;
    permissionDecision?: "allow" | "deny" | "ask";
    permissionDecisionReason?: string;
    additionalContext?: string;
  };
  decision?: "block";
  reason?: string;
};

export function toClaudeHookResponse(
  eventType: AgentEvent["eventType"],
  decision: PolicyDecision
): ClaudeHookResponse;
```

From packages/edge/src/store.ts:
```typescript
export class LocalSqliteStore {
  saveEvent(event: AgentEvent): void;
  saveFindings(findings: SecurityFinding[]): void;
  recentFindings(limit?: number): SecurityFinding[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Finalize hook response envelopes for all policy decisions</name>
  <files>packages/core/src/hooks.ts, packages/edge/src/server.ts, packages/edge/src/server.test.ts</files>
  <behavior>
    - `PreToolUse` denies and asks surface through `hookSpecificOutput.permissionDecision` with a clear reason.
    - Prompt-submit or post-tool deny cases return the correct Claude-compatible stop/block envelope rather than a generic JSON error.
    - Redact decisions suppress output while preserving the sanitized audit trail.
  </behavior>
  <action>Implement D-07 on top of the decision contract from 02-01. Tighten the hook-response mapping so SEC-06 is satisfied for each supported Claude event type already normalized in core. Keep the response format authoritative in `packages/core/src/hooks.ts`; the edge route should delegate to that adapter rather than build custom response objects inline.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge typecheck && pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Edge hook tests prove the route returns valid decision envelopes for deny, ask, redact, and allow paths without schema drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist canonical sanitized findings for hook events</name>
  <files>packages/edge/src/store.ts, packages/edge/src/server.ts, packages/edge/src/server.test.ts</files>
  <behavior>
    - Hook-triggered findings persist with UUID, timestamp, eventId, type, severity, title, description, evidence snippet, and recommendation.
    - Stored evidence is redacted and never stores raw credentials when `capture.rawPayload` is false.
    - Synthetic hook fixtures can assert all expected findings directly from SQLite-backed queries.
  </behavior>
  <action>Implement the SEC-07 persistence guarantees per D-02 and D-08. Keep `LocalSqliteStore` as the only persistence boundary, and add focused tests that assert the stored `security_findings` rows after policy-triggering hook requests. Do not expand this plan into dashboard summary work; only the hook-to-SQLite enforcement path belongs here.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge typecheck && pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Hook requests produce persisted sanitized findings with the required schema fields and no raw-secret leakage in SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude hook HTTP route -> core hook adapter | Untrusted hook JSON must map to valid response envelopes without crashing or drifting from Claude format |
| edge route -> SQLite store | Audit records cross into persistent storage and must remain sanitized |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | `packages/core/src/hooks.ts` | mitigate | Centralize response formatting in one adapter and verify each event-type envelope in tests |
| T-02-05 | I | `packages/edge/src/store.ts` | mitigate | Persist only redacted evidence snippets and rely on `capture.rawPayload` gating before any raw payload storage |
| T-02-06 | R | `packages/edge/src/server.ts` | mitigate | Ensure findings persist with stable IDs and event linkage so denials/asks are auditable after the request completes |
</threat_model>

<verification>
- `pnpm --filter @bastion/edge typecheck`
- `pnpm --filter @bastion/edge test`
</verification>

<success_criteria>
1. Claude hook responses are valid for every Security Core decision type.
2. Hook-triggered findings persist to SQLite with the required schema fields and redacted evidence.
3. The hook path remains focused on enforcement and persistence only, with no Phase 3 spillover.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-core/02-02-SUMMARY.md`.
</output>
