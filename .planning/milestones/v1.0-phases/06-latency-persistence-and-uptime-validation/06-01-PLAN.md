---
phase: 06-latency-persistence-and-uptime-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/edge/src/telemetry/latency.ts
  - packages/edge/src/store.ts
  - packages/edge/src/server.ts
  - packages/edge/src/server.test.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/06-latency-persistence-and-uptime-validation/06-01-SUMMARY.md
  - .planning/phases/06-latency-persistence-and-uptime-validation/06-VERIFICATION.md
autonomous: true
requirements:
  - DOG-02
must_haves:
  truths:
    - "Hook latency samples persist in SQLite and survive edge restarts"
    - "Latency tracker initializes from persisted hook samples"
    - "Latency continuity behavior is covered by automated tests"
  artifacts:
    - path: "packages/edge/src/server.ts"
      provides: "Persisted hook latency write path and startup backfill"
    - path: "packages/edge/src/store.ts"
      provides: "SQLite query for recent persisted hook latency samples"
    - path: "packages/edge/src/server.test.ts"
      provides: "Restart backfill regression coverage"
---

<objective>
Implement restart-safe hook latency continuity so DOG-02 measurements are not reset by server restarts.

Purpose: The milestone audit identified in-memory-only latency tracking as the key DOG-02 integration gap.
Output: Persisted latency samples, startup backfill in /api/latency path, and passing automated regression coverage.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Persist hook latency on ingest</name>
  <files>packages/edge/src/server.ts</files>
  <action>When handling /api/hooks/claude, compute latency for valid hook payloads and store it on the persisted event so hook performance can be recovered after restart.</action>
  <done>Hook events persisted from /api/hooks/claude include latencyMs in SQLite.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill startup latency tracker from SQLite</name>
  <files>packages/edge/src/store.ts, packages/edge/src/telemetry/latency.ts, packages/edge/src/server.ts</files>
  <action>Add a store query for recent persisted hook latencies and initialize the in-memory hook latency tracker with those samples at app startup.</action>
  <done>/api/latency snapshot starts from persisted hook history instead of zero after restart.</done>
</task>

<task type="auto">
  <name>Task 3: Add restart continuity regression test</name>
  <files>packages/edge/src/server.test.ts</files>
  <action>Add a test that seeds persisted hook latency rows, creates edge app, and verifies /api/latency reflects seeded count and aggregates before any new hook request.</action>
  <done>Automated test proves restart continuity for hook latency metrics.</done>
</task>

</tasks>

<verification>
- pnpm --filter @bastion/edge test
</verification>

<success_criteria>
1. Hook latency samples survive restarts through SQLite persistence.
2. /api/latency reflects persisted samples immediately after startup.
3. Regression test coverage protects restart continuity behavior.
</success_criteria>

<output>
After execution, create:
- .planning/phases/06-latency-persistence-and-uptime-validation/06-01-SUMMARY.md
- .planning/phases/06-latency-persistence-and-uptime-validation/06-VERIFICATION.md
</output>