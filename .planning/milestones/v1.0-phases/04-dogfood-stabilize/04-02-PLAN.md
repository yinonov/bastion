---
phase: 04-dogfood-stabilize
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [packages/cli/src/test-threats.ts, packages/cli/src/index.ts]
autonomous: true
requirements: [DOG-01]
must_haves:
  truths:
    - "bastion test-threats command runs and injects seeded threat payloads into the local edge server"
    - "Each threat vector (fake AWS key, dangerous command, protected path) is validated to produce a security finding in SQLite"
    - "Dashboard shows findings from seeded threats, proving detection works before relying on organic catches"
  artifacts:
    - path: "packages/cli/src/test-threats.ts"
      provides: "Test harness to inject synthetic threat vectors"
      exports: ["testThreats"]
    - path: "packages/cli/src/index.ts"
      provides: "CLI command 'bastion test-threats' handler"
      pattern: "program.*command.*test-threats"
  key_links:
    - from: "CLI test-threats command"
      to: "edge /api/hooks/claude"
      via: "POST with synthetic payloads"
    - from: "edge hook handler"
      to: "SQLite findings table"
      via: "saveFindings after policy evaluation"
---

<objective>
Create seeded threat test vectors to prove threat detection works before organic dogfood usage.

Purpose: DOG-01 requires "Bastion catches at least one real secret or dangerous command in the wild." Before relying on organic catches during the 7-day window, the founder needs confidence the system works. This plan provides a `bastion test-threats` command that injects realistic but synthetic payloads (fake AWS keys, fake dangerous commands, etc.) via the hook API and validates each produces a security finding.

Output: Executable `bastion test-threats` CLI command; documented threat vectors; validation script confirming detection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-dogfood-stabilize/04-CONTEXT.md

Existing threat detection from Phases 1-2:
- `packages/core/src/policy.ts` implements `evaluatePolicy` with secret/command/path detection
- Policy rules are tested in `packages/core/src/policy.test.ts`
- Hook handler is in `packages/edge/src/server.ts` at POST `/api/hooks/claude`
- Security findings are persisted to SQLite via `store.saveFindings()`

Test payload reference:
- Fake AWS key format: `AKIAIOSFODNN7EXAMPLE` (public test key pattern)
- Fake GitHub token format: `ghp_XXXXXX` (fake personal access token)
- Dangerous commands: `rm -rf /`, `git push --force`, `DROP DATABASE`
- Protected paths: `.env`, `~/.ssh`, `~/.aws/credentials`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test harness module with seeded threat vectors</name>
  <files>packages/cli/src/test-threats.ts</files>
  <action>Create a new module `test-threats.ts` that exports async function `testThreats(edgeUrl: string, verbose?: boolean): Promise<TestResult[]>`. Implement realistic but synthetic threat vectors:

1. **Secret Detection Vector:** POST to `/api/hooks/claude` with a payload containing a fake AWS key `AKIAIOSFODNN7EXAMPLE` in tool arguments. Expect policy evaluation to produce a secret finding with type `aws_key`.

2. **Dangerous Command Vector:** POST with a PreToolUse event for a `Bash` tool containing `rm -rf /tmp/test`. Expect policy evaluation to produce a finding with type `dangerous_command`.

3. **Protected Path Vector:** POST with a tool call touching `~/.ssh/config`. Expect policy evaluation to produce a finding with type `protected_path`.

For each vector:
- Generate a unique event ID (UUID)
- Post to the edge's `/api/hooks/claude` endpoint (provided as `edgeUrl`)
- Capture the response decision (should be deny or ask)
- Query the running edge server's SQLite store indirectly (or parse response metadata) to confirm a finding was created
- Return structured test result: `{ vector: string, passed: boolean, error?: string }`

Keep payloads synthetic:
- Fake AWS key uses the public test pattern (AKIA...EXAMPLE), not a real key
- No actual credentials or files touched
- Use clear prefixes like `[TEST]` in redacted snippets for traceability

Include verbose logging if `verbose: true` to help founder debug if a vector fails.</action>
  <verify>
    <automated>grep -c "export.*testThreats" packages/cli/src/test-threats.ts && grep -c "AKIAIOSFODNN7EXAMPLE\|rm -rf\|protected_path" packages/cli/src/test-threats.ts</automated>
  </verify>
  <done>Exported `testThreats()` function with 3+ seeded threat vectors; each vector posts synthetic payload to edge and validates detection.</done>
</task>

<task type="auto">
  <name>Task 2: Wire test-threats command into CLI and call the harness</name>
  <files>packages/cli/src/index.ts</files>
  <action>Add a new Commander command to the CLI:

```
program
  .command("test-threats")
  .description("Run seeded threat detection test vectors against the running edge server")
  .option("--edge-url <url>", "edge server URL (default from config or http://localhost:4711)")
  .option("--verbose", "show detailed output for each vector")
  .action(async (options: { edgeUrl?: string; verbose?: boolean }) => {
    // Resolve edge URL: use --edge-url flag if provided, else from config.edge.url, else default
    const edgeUrl = options.edgeUrl ?? (await loadConfig()).edge.url ?? "http://localhost:4711";
    const { testThreats } = await import("./test-threats.js");
    const results = await testThreats(edgeUrl, options.verbose);
    
    // Print summary
    const passed = results.filter(r => r.passed).length;
    const total = results.length;
    console.log(`\nThreat detection test vectors: ${passed}/${total} passed`);
    
    if (options.verbose || passed < total) {
      results.forEach(r => {
        const status = r.passed ? "✓" : "✗";
        console.log(`  ${status} ${r.vector}: ${r.error ?? "OK"}`);
      });
    }
    
    if (passed < total) {
      process.exit(1);
    }
  });
```

Ensure the command is registered before `program.parse(process.argv)` at the end of the file.</action>
  <verify>
    <automated>grep -c "test-threats" packages/cli/src/index.ts && grep -c "testThreats" packages/cli/src/index.ts</automated>
  </verify>
  <done>CLI command `bastion test-threats` registered and wired to call `testThreats()` harness; supports --edge-url and --verbose options.</done>
</task>

<task type="auto">
  <name>Task 3: Document test vectors and success criteria</name>
  <files>packages/cli/src/test-threats.ts</files>
  <action>Add JSDoc comment block above the `testThreats()` function documenting:
- What each test vector does (secret, command, path)
- Expected outcomes (denial, finding severity)
- How to interpret results
- Example: `bastion test-threats` should output "3/3 passed" if threat detection is working

Include inline comments in the vector implementations explaining the synthetic payload choice (why it's safe, why it matches real patterns, etc.). No additional file creation needed; documentation lives in the code comments.</action>
  <verify>
    <automated>grep -c "Test vector\|Expected\|@param\|@returns" packages/cli/src/test-threats.ts</automated>
  </verify>
  <done>Test vectors are documented with JSDoc and inline comments; founder can understand what each vector tests and why.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test vectors → Edge server | Synthetic payloads contain no real credentials; fake keys use public test patterns only |
| Test vectors → Policy engine | Policy engine processes test payloads same as real events; no special handling needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Spoofing | Fake AWS key in test payload | accept | Key uses public test pattern (AKIA...EXAMPLE) that is not a valid credential. Even if stored in findings, it cannot be used to access AWS. |
| T-04-02 | Information Disclosure | Test results show which threats were caught | accept | Results are printed to founder's terminal only; no data sent off-machine. Confirms security posture locally. |

</threat_model>

<verification>
1. CLI command `bastion test-threats` exists and is callable
2. Command resolves edge URL correctly from flag, config, or default
3. Each test vector posts to the edge and receives a response
4. Responses indicate policy decisions (deny/ask/allow as expected)
5. Findings appear in SQLite after test run (queryable from dashboard)
6. `bastion test-threats --verbose` shows all 3+ vectors with pass/fail status
7. Exit code is 0 if all vectors passed, 1 if any failed
</verification>

<success_criteria>
- [ ] `bastion test-threats` command runs without errors
- [ ] All 3+ seeded threat vectors are tested and pass
- [ ] Dashboard shows findings from test vectors (identifiable by `[TEST]` prefix or metadata)
- [ ] Founder can run `bastion test-threats` repeatedly with consistent results
- [ ] No false negatives: each threat vector is correctly detected
</success_criteria>

<output>
After completion, create `.planning/phases/04-dogfood-stabilize/04-02-SUMMARY.md` with:
- Files modified list
- Commits created (3 for 3 tasks)
- Test vector validation results
- Any vectors that failed or required adjustment
- Verification checklist results
</output>
