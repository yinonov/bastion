---
phase: 03-intelligence-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/edge/src/store.ts
  - packages/edge/src/store.test.ts
  - packages/edge/src/server.ts
autonomous: true
requirements:
  - INS-01
  - INS-03
  - DASH-05
must_haves:
  truths:
    - "Friction clusters are computed from persisted events and saved to SQLite after event/finding ingestion batches"
    - "Developer insights are generated from real events/findings and persisted to SQLite with stable schema fields"
    - "Dashboard summary reads clusters/insights from durable SQLite-backed data instead of ephemeral placeholder structures"
  artifacts:
    - path: "packages/edge/src/store.ts"
      provides: "SQLite schema + persistence/read APIs for friction clusters and developer insights"
    - path: "packages/edge/src/store.test.ts"
      provides: "Automated proof that insights/clusters persist and round-trip through dashboardSummary"
    - path: "packages/edge/src/server.ts"
      provides: "Edge route wiring that exposes persisted summary/events/findings data to dashboard consumers"
  key_links:
    - from: "packages/edge/src/store.ts"
      to: "packages/insights/src/index.ts"
      via: "buildFrictionClusters + generateDeveloperInsights over persisted event/finding windows"
      pattern: "persisted clusters/insights are read back by dashboardSummary"
    - from: "packages/edge/src/server.ts"
      to: "packages/edge/src/store.ts"
      via: "/api/summary, /api/events, /api/findings handlers"
      pattern: "dashboard endpoints return live SQLite-backed objects"
---

<objective>
Create the SQLite-backed intelligence foundation so clusters and developer insights are generated from real event/finding data and persisted for dashboard use.

Purpose: Deliver the durable data layer required for INS-01, INS-03, and DASH-05 without introducing mock or transient-only insight data.
Output: Updated store schema/methods, edge route wiring, and focused persistence tests.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-intelligence-dashboard/03-CONTEXT.md
@packages/edge/src/store.ts
@packages/edge/src/server.ts
@packages/insights/src/index.ts

<interfaces>
From packages/edge/src/store.ts:
```typescript
dashboardSummary(limit = 250): DashboardSummary {
  const events = this.recentEvents(limit);
  const findings = this.recentFindings(limit);
  const clusters = buildFrictionClusters(events);
  const insights = generateDeveloperInsights({ events, findings });
  // ...totals
}
```

From packages/insights/src/index.ts:
```typescript
export function buildFrictionClusters(events: AgentEvent[]): FrictionCluster[];
export function generateDeveloperInsights(input: { events: AgentEvent[]; findings: SecurityFinding[] }): DeveloperInsight[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add persistent SQLite tables and store APIs for clusters and insights</name>
  <files>packages/edge/src/store.ts</files>
  <behavior>
    - Friction clusters computed from recent persisted events are written to SQLite with deterministic identifiers or replacement strategy.
    - Developer insights computed from persisted events/findings are written to SQLite and queryable in recency order.
    - Dashboard summary reads persisted clusters/insights instead of computing ad hoc values at response time.
  </behavior>
  <action>Implement D-04 by extending `LocalSqliteStore.migrate()` with explicit `friction_clusters` and `developer_insights` tables and indexes, then add `saveClustersAndInsights`/`recentClusters`/`recentInsights` helpers and wire `dashboardSummary()` to read those persisted records. Keep schema/runtime validation through existing core Zod contracts and preserve existing event/finding behavior unchanged.</action>
  <verify>
    <automated>MISSING - Wave 0 must create packages/edge/src/store.test.ts first</automated>
  </verify>
  <done>Store exposes deterministic read/write paths for clusters/insights and dashboardSummary returns persisted intelligence data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add persistence tests for intelligence round-trip integrity</name>
  <files>packages/edge/src/store.test.ts</files>
  <behavior>
    - Synthetic denied/failed event batches produce persisted friction clusters with expected signals and occurrence counts.
    - Generated developer insights are persisted and returned by dashboardSummary with valid schema fields.
    - No mock or hardcoded cluster/insight payloads are needed in tests.
  </behavior>
  <action>Create a new co-located store test suite (per D-07) using temp SQLite files to seed events/findings, invoke the new store intelligence persistence path, and assert `dashboardSummary()` returns the persisted clusters/insights. Keep tests package-local and deterministic; avoid depending on dashboard UI tests in this plan.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Edge tests prove clusters and insights persist in SQLite and are visible through summary reads.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve live dashboard endpoint contracts over persisted intelligence data</name>
  <files>packages/edge/src/server.ts</files>
  <action>Apply D-02 and D-07 by ensuring `/api/summary`, `/api/events`, and `/api/findings` continue to source from `LocalSqliteStore` after persistence changes and that no route introduces mock/fallback payload fabrication. Keep response shape backward-compatible for existing dashboard consumers.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Edge read endpoints serve live SQLite-backed data with unchanged API contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge hook ingress -> SQLite intelligence tables | Untrusted hook-derived content crosses into persisted analytics state |
| dashboard HTTP reads -> edge store | Dashboard consumers trust edge responses to represent real persisted governance data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `packages/edge/src/store.ts` intelligence writes | mitigate | Validate persisted cluster/insight records against core schemas before insert/update to prevent malformed analytics state |
| T-03-02 | I | `packages/edge/src/server.ts` summary route | mitigate | Continue returning sanitized event/finding/insight fields only; never expose raw payload content through dashboard endpoints |
| T-03-03 | D | `packages/edge/src/store.ts` summary generation | mitigate | Add indexes and bounded recent-window reads so intelligence queries remain bounded and do not starve hook-serving resources |
</threat_model>

<verification>
- `pnpm --filter @bastion/edge test`
</verification>

<success_criteria>
1. SQLite contains durable friction cluster and developer insight records generated from real events/findings.
2. `dashboardSummary()` returns persisted intelligence data aligned with INS-01/INS-03 semantics.
3. Edge read endpoints continue serving live SQLite-backed data with no fabricated placeholder responses.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-dashboard/03-01-SUMMARY.md`.
</output>
