---
phase: 03-intelligence-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - apps/dashboard/lib/types.ts
  - apps/dashboard/lib/api.ts
  - apps/dashboard/app/page.tsx
  - apps/dashboard/components/live-dashboard.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-03
  - DASH-04
must_haves:
  truths:
    - "Dashboard data comes from live edge endpoints (/api/summary, /api/events, /api/findings) with no silent mock totals fallback"
    - "Live event stream updates on a 2-second cadence and shows correct status, event type, and tool name"
    - "Security findings panel renders real SQLite-backed findings data with correct type/severity/description"
  artifacts:
    - path: "apps/dashboard/lib/api.ts"
      provides: "Live endpoint client for summary/events/findings with explicit unavailable state"
    - path: "apps/dashboard/components/live-dashboard.tsx"
      provides: "Client-side polling renderer for event stream and findings panels"
    - path: "apps/dashboard/app/page.tsx"
      provides: "Server/client wiring that hydrates initial live data and renders realtime updates"
  key_links:
    - from: "apps/dashboard/lib/api.ts"
      to: "packages/edge/src/server.ts"
      via: "HTTP calls to /api/summary, /api/events, /api/findings"
      pattern: "no mock/hardcoded totals returned on fetch failure"
    - from: "apps/dashboard/components/live-dashboard.tsx"
      to: "apps/dashboard/lib/types.ts"
      via: "typed polling state and render model"
      pattern: "2s refresh updates stream/findings without full page reload"
---

<objective>
Finish Phase 3 by wiring the dashboard UI to live edge data streams and removing fallback behavior that can masquerade as real telemetry.

Purpose: Satisfy DASH-01, DASH-03, and DASH-04 with production-real data flow from the running edge server.
Output: Updated dashboard API/types, a client polling component, and page wiring that renders true live stream and findings data.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-intelligence-dashboard/03-CONTEXT.md
@.planning/phases/03-intelligence-dashboard/03-02-PLAN.md
@apps/dashboard/lib/types.ts
@apps/dashboard/lib/api.ts
@apps/dashboard/app/page.tsx

<interfaces>
From apps/dashboard/lib/api.ts:
```typescript
export async function getDashboardSummary(): Promise<DashboardSummary> {
  try {
    const response = await fetch(`${edgeUrl}/api/summary`, { cache: "no-store" });
    // ...
  } catch {
    return emptySummary();
  }
}
```

From apps/dashboard/app/page.tsx:
```typescript
const summary = await getDashboardSummary();
const latestEvents = summary.events.slice(0, 8);
const findings = summary.findings.slice(0, 6);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce explicit live dashboard data contract and remove fabricated fallback summaries</name>
  <files>apps/dashboard/lib/types.ts, apps/dashboard/lib/api.ts</files>
  <behavior>
    - Dashboard API client fetches summary/events/findings from edge endpoints and surfaces an explicit unavailable/error state.
    - API client no longer returns an auto-generated all-zero summary that can be mistaken for live telemetry.
    - Types capture initial snapshot plus refresh payload shape for stream/findings updates.
  </behavior>
  <action>Implement D-02 and D-03 by replacing `emptySummary()` fallback semantics with a typed `DashboardUnavailable` path and adding endpoint clients for `/api/events` and `/api/findings` alongside `/api/summary`. Keep edge URL configurability (`BASTION_EDGE_URL`) intact and preserve no-store fetch behavior.</action>
  <verify>
    <automated>pnpm --filter @bastion/dashboard typecheck</automated>
  </verify>
  <done>Dashboard data layer distinguishes real live data from unavailable-edge state and supports all required live endpoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add client polling renderer for 2-second live event/finding updates</name>
  <files>apps/dashboard/components/live-dashboard.tsx, apps/dashboard/app/page.tsx</files>
  <behavior>
    - UI renders initial server-fetched snapshot, then polls every 2 seconds for fresh events/findings.
    - Live stream continues to show last 8 events with status, event type/tool name, and severity badge integrity.
    - Findings panel updates from real endpoint payloads without placeholder substitution.
  </behavior>
  <action>Create a new client component that receives initial summary/events/findings data, performs 2-second refresh polling (D-03), and updates stream/finding sections in-place. Wire `app/page.tsx` to provide initial data and unavailable-state messaging while preserving existing panel visual language.</action>
  <verify>
    <automated>pnpm --filter @bastion/dashboard typecheck && pnpm --filter @bastion/dashboard build</automated>
  </verify>
  <done>Dashboard renders real, refreshing stream and findings data at 2-second cadence with no mock fallback path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser dashboard client -> local edge API | UI consumes untrusted network responses that must not be confused with mocked local defaults |
| server component -> client polling component | Data handoff across hydration boundary must preserve truthful state and error semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | S | `apps/dashboard/lib/api.ts` data source trust | mitigate | Expose explicit unavailable state and endpoint errors rather than returning fabricated zero-summary payloads |
| T-03-08 | T | `apps/dashboard/components/live-dashboard.tsx` polling updates | mitigate | Validate payload shape through typed parsing/guards before updating rendered event/finding state |
| T-03-09 | D | `apps/dashboard/components/live-dashboard.tsx` refresh loop | mitigate | Use bounded 2-second polling with cleanup on unmount to avoid runaway requests and UI resource exhaustion |
</threat_model>

<verification>
- `pnpm --filter @bastion/dashboard typecheck`
- `pnpm --filter @bastion/dashboard build`
</verification>

<success_criteria>
1. Dashboard consumes `/api/summary`, `/api/events`, and `/api/findings` with explicit error/unavailable handling.
2. Live stream and findings panels update every 2 seconds using real edge data.
3. No hardcoded/mock summary fallback remains in dashboard data access.
</success_criteria>

<output>
After completion, create `.planning/phases/03-intelligence-dashboard/03-03-SUMMARY.md`.
</output>
