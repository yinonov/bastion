---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/storage/local-sqlite-store.ts
  - packages/core/src/config/load-config.ts
  - packages/core/src/config/defaults.ts
  - packages/edge/src/server.ts
  - packages/edge/src/hooks/route-hook-event.ts
  - packages/edge/src/telemetry/latency.ts
  - packages/cli/src/commands/install-hooks.ts
  - packages/cli/src/commands/start-edge.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
must_haves:
  truths:
    - "Global install succeeds without native compilation friction"
    - "Hook events are forwarded to edge over HTTP and receive valid responses"
    - "Hook path p95 latency is measured and <=50ms under normal load"
    - "Edge startup rejects duplicate instances and malformed payloads do not crash the process"
    - "Config resolution works from CWD then ~/.config/bastion with defaults"
  artifacts:
    - path: "packages/core/src/storage/local-sqlite-store.ts"
      provides: "node:sqlite-backed local store with WAL-safe behavior"
    - path: "packages/edge/src/server.ts"
      provides: "stable edge HTTP runtime with duplicate-instance guard"
    - path: "packages/edge/src/hooks/route-hook-event.ts"
      provides: "hook event HTTP entry and response mapping"
    - path: "packages/cli/src/commands/install-hooks.ts"
      provides: "non-destructive Claude hooks config writer"
    - path: "packages/core/src/config/load-config.ts"
      provides: "deterministic config discovery and defaults"
  key_links:
    - from: "packages/cli/src/commands/install-hooks.ts"
      to: "Claude hooks config file"
      via: "merge/update write"
      pattern: "install-hooks writes merged hook command targeting edge URL"
    - from: "Claude hook process"
      to: "packages/edge/src/hooks/route-hook-event.ts"
      via: "HTTP POST"
      pattern: "valid JSON request/response contract"
    - from: "packages/edge/src/hooks/route-hook-event.ts"
      to: "packages/edge/src/telemetry/latency.ts"
      via: "hot-path timing instrumentation"
      pattern: "p95 latency metric recorded"
---

<objective>
Deliver Phase 1 Foundation so Bastion is installable and operational with measured hook latency and robust process behavior.

Purpose: Unblock all subsequent phases by establishing a stable, low-latency local runtime.
Output: Updated core/edge/cli foundation paths implementing INFRA-01..06.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace native SQLite dependency path and finalize config discovery contracts</name>
  <files>packages/core/src/storage/local-sqlite-store.ts, packages/core/src/config/load-config.ts, packages/core/src/config/defaults.ts</files>
  <behavior>
    - Store initializes using Node 22 `node:sqlite` APIs with WAL pragmas and no native addon assumptions.
    - Config loader resolves from CWD first, then `~/.config/bastion/`, and creates sane defaults when file is absent.
    - Existing call sites compile without requiring `better-sqlite3` types or runtime.
  </behavior>
  <action>Implement storage and config updates per D-01 and D-05. Remove or isolate any native-binding assumptions so `npm install -g @bastion/cli` does not require `node-gyp`. Ensure defaults are deterministic and covered by tests.</action>
  <verify>
    <automated>pnpm --filter @bastion/core typecheck && pnpm --filter @bastion/core test</automated>
  </verify>
  <done>Core package builds/tests pass with `node:sqlite` path and config discovery order matches INFRA-01 and INFRA-06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire hook transport over edge HTTP with latency instrumentation and resilience</name>
  <files>packages/edge/src/server.ts, packages/edge/src/hooks/route-hook-event.ts, packages/edge/src/telemetry/latency.ts</files>
  <behavior>
    - Hook route accepts expected Claude hook payload shape and returns valid hook response JSON.
    - Malformed payloads return controlled error responses and never crash the process.
    - p95 latency for hook routing is measured and emitted for verification.
    - Duplicate edge instance start is rejected with explicit error output.
  </behavior>
  <action>Implement transport and runtime hardening per D-02 and D-04. Keep expensive writes or analysis off the hot path so INFRA-04 remains achievable. Add explicit duplicate-instance guard at startup and add route-level malformed-input handling.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge typecheck && pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Edge server demonstrates stable startup, malformed-payload safety, duplicate-instance rejection, and measurable latency required by INFRA-03, INFRA-04, and INFRA-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden CLI hook installer to produce valid merged Claude hooks config</name>
  <files>packages/cli/src/commands/install-hooks.ts, packages/cli/src/commands/start-edge.ts</files>
  <behavior>
    - `bastion install-hooks` writes a valid hooks config entry that targets running edge HTTP endpoint.
    - Existing hooks config data is preserved unless explicitly replaced.
    - Re-running installer is idempotent and does not duplicate entries.
  </behavior>
  <action>Implement hook config merge behavior per D-03 and ensure CLI startup wiring aligns with D-02. Include a dry-run-friendly structure so future `--dry-run` can be added without redesign.</action>
  <verify>
    <automated>pnpm --filter @bastion/cli typecheck && pnpm --filter @bastion/cli test</automated>
  </verify>
  <done>CLI writes a valid and merge-safe hooks config and satisfies INFRA-02 without destructive edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> local filesystem | Hook/config writes must not corrupt existing user files |
| Claude hook process -> edge API | Untrusted JSON payload crosses into server process |
| edge API -> SQLite | Persisted event data must not destabilize runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `install-hooks` config writer | mitigate | Atomic write + merge semantics + JSON validation before replace |
| T-01-02 | D | hook route handler | mitigate | Input schema validation and guarded error path for malformed payloads |
| T-01-03 | D | edge process startup | mitigate | Duplicate-instance detection and clear non-zero exit with message |
| T-01-04 | I | persisted hook evidence | mitigate | Do not log raw malformed payload bodies in error responses |
</threat_model>

<verification>
- `pnpm --filter @bastion/core test`
- `pnpm --filter @bastion/edge test`
- `pnpm --filter @bastion/cli test`
- `pnpm typecheck`
</verification>

<success_criteria>
1. Install and runtime foundation covers INFRA-01..06 with explicit verification hooks.
2. Plan tasks are executable without interpretation and aligned to locked decisions in context.
3. Hot-path latency instrumentation exists and is usable for <=50ms validation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`.
</output>
