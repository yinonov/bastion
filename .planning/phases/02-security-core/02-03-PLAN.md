---
phase: 02-security-core
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - packages/edge/package.json
  - pnpm-lock.yaml
  - packages/edge/src/server.ts
  - packages/edge/src/server.test.ts
autonomous: true
requirements:
  - SEC-03
  - MCP-01
  - MCP-02
  - MCP-03
must_haves:
  truths:
    - "Requests to unapproved MCP servers return a valid JSON-RPC -32003 error and are persisted as findings"
    - "Approved HTTP MCP servers stream through the proxy without buffered fetch artifacts"
    - "Policy-denied approved-server MCP requests return JSON-RPC -32004 without crashing the client"
  artifacts:
    - path: "packages/edge/package.json"
      provides: "Streaming MCP proxy dependency declaration"
    - path: "packages/edge/src/server.ts"
      provides: "MCP allowlist gate, policy evaluation, JSON-RPC errors, and streaming proxy behavior"
    - path: "packages/edge/src/server.test.ts"
      provides: "Executable coverage for MCP deny, allow, and upstream failure paths"
  key_links:
    - from: "packages/edge/src/server.ts"
      to: "bastion.config.json mcp.servers"
      via: "allowlist lookup by serverName"
      pattern: "unapproved -> -32003, approved policy deny -> -32004"
    - from: "packages/edge/src/server.ts"
      to: "approved HTTP upstream"
      via: "@fastify/reply-from streaming proxy"
      pattern: "response body is streamed, not buffered through fetch().text()"
---

<objective>
Complete the MCP security surface with allowlist enforcement, policy checks, valid JSON-RPC failures, and streaming proxy behavior for approved HTTP servers.

Purpose: Finish Phase 2 by securing the MCP ingress path without regressing the local governance model established in earlier plans.
Output: Updated edge package dependency, MCP proxy route, and tests that prove allowlist, denial, streaming, and failure behavior.
</objective>

<execution_context>
@/Users/yinon/.copilot/get-shit-done/workflows/execute-plan.md
@/Users/yinon/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-security-core/02-CONTEXT.md
@.planning/phases/02-security-core/02-01-PLAN.md
@.planning/phases/02-security-core/02-02-PLAN.md
@packages/edge/package.json
@packages/edge/src/server.ts

<interfaces>
From packages/edge/src/server.ts:
```typescript
app.post("/mcp/:serverName", async (request, reply) => {
  // current path: allowlist check -> evaluatePolicy(event, config) -> buffered fetch/jsonRpcError
});
```

Current JSON-RPC helpers already present:
```typescript
function jsonRpcError(body: unknown, code: number, message: string): Record<string, unknown> {
  const record = asRecord(body);
  return {
    jsonrpc: "2.0",
    id: record.id ?? null,
    error: { code, message }
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace buffered MCP forwarding with streaming reply-from proxying</name>
  <files>packages/edge/package.json, pnpm-lock.yaml, packages/edge/src/server.ts, packages/edge/src/server.test.ts</files>
  <behavior>
    - Approved HTTP MCP upstreams proxy through `@fastify/reply-from` without `fetch(...).text()` buffering.
    - Existing JSON-RPC identity fields and upstream status/content-type behavior remain intact.
    - The implementation keeps stdio upstreams explicitly unsupported in v1.
  </behavior>
  <action>Implement D-06 using `@fastify/reply-from` rather than the current buffered fetch path. Update package metadata and lockfile as needed. Preserve the existing HTTP-only scope and keep the route logic readable enough that allowlist and policy-denial branches still happen before proxying. Do not introduce a generic transport abstraction in this phase.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge typecheck && pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>Approved HTTP MCP requests are streamed through the proxy and tests prove the route no longer depends on a buffered fetch/text path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce MCP allowlist and denial envelopes with persisted findings</name>
  <files>packages/edge/src/server.ts, packages/edge/src/server.test.ts</files>
  <behavior>
    - Unapproved servers return JSON-RPC `-32003` and persist an `mcp_server_not_approved` finding.
    - Approved-server requests that fail policy evaluation return JSON-RPC `-32004` and persist the policy findings from core.
    - Upstream failures still return a valid JSON-RPC error without crashing the route.
  </behavior>
  <action>Implement SEC-03, MCP-02, and MCP-03 on top of the core decisions from 02-01 and the SQLite path from 02-02. Keep the allowlist source in `bastion.config.json`, return valid JSON-RPC envelopes for each failure branch, and assert persisted findings directly from the backing store in tests. Avoid Phase 3 dashboard work and avoid changing the config discovery contract from Phase 1.</action>
  <verify>
    <automated>pnpm --filter @bastion/edge typecheck && pnpm --filter @bastion/edge test</automated>
  </verify>
  <done>MCP tests prove unapproved-server denials, policy denials, upstream failures, and approved streaming behavior with persisted findings and valid JSON-RPC envelopes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local MCP client -> edge proxy | Potentially dangerous tool requests cross into Bastion governance before upstream execution |
| edge proxy -> approved HTTP MCP server | Approved traffic must be streamed safely without bypassing policy gates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | E | `packages/edge/src/server.ts` MCP route | mitigate | Enforce allowlist before any upstream proxy call and return `-32003` for unapproved servers |
| T-02-08 | T | `packages/edge/src/server.ts` MCP route | mitigate | Run `evaluatePolicy` before proxying and map denies to JSON-RPC `-32004` without leaking execution to upstream |
| T-02-09 | D | streaming proxy path | mitigate | Use `@fastify/reply-from` to avoid buffered artifacts and preserve controlled upstream failure handling |
</threat_model>

<verification>
- `pnpm --filter @bastion/edge typecheck`
- `pnpm --filter @bastion/edge test`
</verification>

<success_criteria>
1. MCP allowlist and policy denial branches return valid JSON-RPC errors with persisted findings.
2. Approved HTTP servers proxy through a streaming path rather than buffered fetch.
3. Stdio remains explicitly out of scope and does not silently fall through to partial support.
</success_criteria>

<output>
After completion, create `.planning/phases/02-security-core/02-03-SUMMARY.md`.
</output>
